# API / Interfaces

MVP:
- No external API calls.
- All work is done client-side.

Future:
- Optional backend API for:
  - User accounts
  - Cloud sync of tools, templates, and history
  - Integrations with GitHub or project metadata
