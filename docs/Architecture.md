# Architecture

- Frontend: React + Vite + TypeScript.
- No backend for MVP.
- State stored in React + browser localStorage (future enhancement).
- The app runs as a single-page application (SPA) that can be hosted on:
  - GitHub Pages
  - Any static hosting behind Nginx/Traefik
